import { View } from "react-native";
import React from "react";
import Header from "./Header";

const Layout = () => {
  return (
    <View>
      <Header />
    </View>
  );
};

export default Layout;
